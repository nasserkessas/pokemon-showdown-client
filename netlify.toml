[build]
  # We don't run any scripts, just make sure there's something to publish
  command = "echo 'Building...' "
  publish = "."

[[redirects]]
  from = "/showdown-proxy/*"
  to = "https://sim3.psim.us/:splat"
  status = 200
  force = true
